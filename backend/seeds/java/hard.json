[
  {
    "question": "What is the contract between equals() and hashCode()?",
    "options": [
      "Unequal objects must have different hash codes",
      "If two objects are equal, they must have the same hash code",
      "If two objects have the same hash code, they must be equal",
      "There is no formal contract between them"
    ],
    "correctAnswer": 1,
    "explanation": "If `a.equals(b)` is true, `a.hashCode() == b.hashCode()` must also be true. The reverse is not required."
  },
  {
    "question": "In Java memory management, where are static variables stored?",
    "options": [
      "Heap memory",
      "Stack memory",
      "Method Area (Metaspace)",
      "PC Register"
    ],
    "correctAnswer": 2,
    "explanation": "Static variables and class metadata are stored in the Method Area, which is implemented as Metaspace in Java 8+."
  },
  {
    "question": "What happens if a thread calls wait() without holding the object's monitor lock?",
    "options": [
      "The thread sleeps indefinitely",
      "Compile-time error",
      "IllegalMonitorStateException is thrown",
      "The lock is automatically acquired"
    ],
    "correctAnswer": 2,
    "explanation": "Calling `wait()`, `notify()`, or `notifyAll()` without synchronizing on the object throws an `IllegalMonitorStateException` at runtime."
  },
  {
    "question": "Which garbage collector is the default in Java 11 and later?",
    "options": [
      "Serial GC",
      "Parallel GC",
      "G1 GC",
      "ZGC"
    ],
    "correctAnswer": 2,
    "explanation": "The Garbage-First (G1) collector has been the default GC since Java 9."
  },
  {
    "question": "What is Type Erasure in Java Generics?",
    "options": [
      "A process where the compiler removes generic type information at runtime",
      "A security feature that deletes unused types",
      "A runtime error when casting generic collections",
      "A method to clear a collection's memory"
    ],
    "correctAnswer": 0,
    "explanation": "Java uses Type Erasure to ensure backward compatibility. Generic type parameters are replaced with their bounds or `Object` at compile-time."
  },
  {
    "question": "How does the 'volatile' keyword work under the hood?",
    "options": [
      "It locks the object in memory",
      "It forces threads to read from and write to main memory directly",
      "It prevents garbage collection of the variable",
      "It caches the variable in the CPU register"
    ],
    "correctAnswer": 1,
    "explanation": "`volatile` establishes a happens-before relationship, bypassing the thread's local CPU cache to ensure visibility across all threads."
  },
  {
    "question": "What is a memory leak in Java?",
    "options": [
      "When the JVM runs out of Stack memory",
      "When C++ code accessed via JNI crashes",
      "When an object is unreachable but not garbage collected",
      "When objects are no longer used but still referenced by active objects"
    ],
    "correctAnswer": 3,
    "explanation": "Because the Garbage Collector only cleans up unreferenced objects, holding unnecessary references (e.g., in static collections) prevents memory from being freed."
  },
  {
    "question": "Which of the following creates a thread-safe singleton in a multi-threaded environment without synchronization overhead?",
    "options": [
      "Lazy initialization",
      "Double-checked locking",
      "Initialization-on-demand holder idiom",
      "ThreadLocal singleton"
    ],
    "correctAnswer": 2,
    "explanation": "The Initialization-on-demand holder idiom uses a static inner class. The classloader handles the synchronization mechanism implicitly when the inner class is loaded."
  },
  {
    "question": "What is the purpose of the CountDownLatch in java.util.concurrent?",
    "options": [
      "To cycle through a set of threads repeatedly",
      "To wait until a set of operations in other threads completes",
      "To manage thread pool sizes dynamically",
      "To lock a variable during read operations"
    ],
    "correctAnswer": 1,
    "explanation": "A `CountDownLatch` initializes with a count and makes one or more threads wait until the count reaches zero via `countDown()` calls."
  },
  {
    "question": "Can you catch an Error in Java?",
    "options": [
      "No, it bypasses the catch block",
      "Yes, but it is highly discouraged as Errors indicate unrecoverable JVM problems",
      "Yes, but only VirtualMachineError",
      "No, Errors are not subclasses of Throwable"
    ],
    "correctAnswer": 1,
    "explanation": "While structurally possible because `Error` extends `Throwable`, it is bad practice. Errors (like `OutOfMemoryError`) mean the JVM is in a severely degraded state."
  },
  {
    "question": "What is the difference between a daemon thread and a user thread?",
    "options": [
      "Daemon threads run in the background and do not prevent the JVM from exiting",
      "User threads are lower priority than daemon threads",
      "Daemon threads cannot be interrupted",
      "User threads are automatically garbage collected"
    ],
    "correctAnswer": 0,
    "explanation": "Daemon threads are background service threads. The JVM exits when only daemon threads remain, while user threads keep the JVM alive."
  },
  {
    "question": "Which Reference type allows its referent to be collected only when the JVM desperately needs memory to avoid OutOfMemoryError?",
    "options": [
      "WeakReference",
      "SoftReference",
      "PhantomReference",
      "Strong Reference"
    ],
    "correctAnswer": 1,
    "explanation": "`SoftReference` objects are cleared at the discretion of the garbage collector in response to memory demand (usually before an OOM occurs), making them ideal for memory caches."
  },
  {
    "question": "What is the difference between ReentrantLock and the synchronized keyword?",
    "options": [
      "synchronized allows interruptible lock acquisition; ReentrantLock does not",
      "ReentrantLock supports fairness policies and tryLock() timeouts",
      "ReentrantLock cannot be used across multiple methods",
      "synchronized is slower in all scenarios in modern JVMs"
    ],
    "correctAnswer": 1,
    "explanation": "`ReentrantLock` offers advanced features such as timed lock waits (`tryLock`), interruptible locks, and explicit fairness settings that `synchronized` lacks."
  },
  {
    "question": "Why is the `volatile` keyword critical when implementing double-checked locking for Singletons?",
    "options": [
      "It guarantees synchronized access to the instance method",
      "It prevents instruction reordering during object initialization",
      "It enforces memory serialization to disk",
      "It makes constructor execution thread-safe"
    ],
    "correctAnswer": 1,
    "explanation": "Without `volatile`, the JVM/CPU might reorder object allocation and field initialization, allowing another thread to observe a non-null reference to a partially initialized object."
  },
  {
    "question": "In CompletableFuture, how does join() differ from get()?",
    "options": [
      "join() throws checked exceptions, get() throws unchecked exceptions",
      "join() throws unchecked exceptions (CompletionException), while get() throws checked exceptions",
      "join() is asynchronous, get() is synchronous",
      "There is no difference"
    ],
    "correctAnswer": 1,
    "explanation": "`get()` throws checked exceptions (`InterruptedException`, `ExecutionException`), whereas `join()` throws unchecked exceptions (`CompletionException`), making it easier to use inside lambda expressions."
  },
  {
    "question": "How does Java's parent-delegation ClassLoader model work?",
    "options": [
      "ClassLoaders load classes locally first before asking parents",
      "A ClassLoader delegates class loading to its parent before attempting to load it itself",
      "ClassLoaders load classes concurrently from all available jars",
      "ClassLoaders delegate exclusively to the Bootstrap ClassLoader"
    ],
    "correctAnswer": 1,
    "explanation": "When asked to load a class, a ClassLoader delegates to its parent first. Only if the parent fails to find the class does the child attempt to load it."
  },
  {
    "question": "What design strategy does the ForkJoinPool framework use to maximize thread usage?",
    "options": [
      "Round-robin scheduling",
      "Work-stealing algorithm",
      "LIFO stack scheduling",
      "Priority Queue preemptive scheduling"
    ],
    "correctAnswer": 1,
    "explanation": "Idle threads in a `ForkJoinPool` actively 'steal' pending tasks from the double-ended queues (deque) of busy worker threads."
  },
  {
    "question": "What is true about Java Records introduced in Java 14/16?",
    "options": [
      "They are mutable data carriers",
      "They automatically generate final fields, constructor, equals(), hashCode(), and toString()",
      "They can extend other classes",
      "They cannot implement interfaces"
    ],
    "correctAnswer": 1,
    "explanation": "Records are immutable data classes where the compiler automatically generates accessor methods, `equals()`, `hashCode()`, `toString()`, and a canonical constructor."
  },
  {
    "question": "What does a Happens-Before relationship guarantee in the Java Memory Model?",
    "options": [
      "That one operation executes strictly before another in physical clock time",
      "That memory writes by one thread are visible to reads by another thread",
      "That the garbage collector runs before heap exhaustion",
      "That lock-free code will never deadlock"
    ],
    "correctAnswer": 1,
    "explanation": "A Happens-Before relationship ensures memory visibility—meaning changes made by thread A are guaranteed to be seen by thread B at specific sync points."
  },
  {
    "question": "Where is memory allocated when calling ByteBuffer.allocateDirect()?",
    "options": [
      "JVM Heap Memory",
      "Thread Stack Memory",
      "Off-Heap Native Memory",
      "Metaspace"
    ],
    "correctAnswer": 2,
    "explanation": "Direct ByteBuffers allocate native memory outside the normal JVM heap, avoiding copy overhead during native I/O operations."
  },
  {
    "question": "What replaced PermGen (Permanent Generation) starting in Java 8?",
    "options": [
      "Native Heap",
      "Metaspace",
      "G1 Space",
      "Code Cache"
    ],
    "correctAnswer": 1,
    "explanation": "Java 8 replaced PermGen with Metaspace, which expands dynamically using native host memory rather than fixed JVM heap memory."
  },
  {
    "question": "What distinguishes Hashtable from HashMap?",
    "options": [
      "Hashtable allows null keys",
      "Hashtable is synchronized while HashMap is not",
      "HashMap is slower by design",
      "Hashtable preserves insertion order"
    ],
    "correctAnswer": 1,
    "explanation": "Hashtable methods are synchronized, making it thread-safe but slower than HashMap."
  },
  {
    "question": "What is ConcurrentHashMap designed for?",
    "options": [
      "Faster single-threaded iteration",
      "High-concurrency thread-safe map access",
      "Persisting maps to disk",
      "Sorting entries automatically"
    ],
    "correctAnswer": 1,
    "explanation": "ConcurrentHashMap enables safe concurrent reads/writes without full-table locking."
  },
  {
    "question": "Why must hashCode be overridden whenever equals is overridden?",
    "options": [
      "To satisfy the compiler",
      "So equal objects hash equally, keeping hash-based collections correct",
      "To speed up reflection",
      "Because Object requires native calls"
    ],
    "correctAnswer": 1,
    "explanation": "Violating the equals/hashCode contract corrupts HashMap and HashSet lookups."
  },
  {
    "question": "What does the volatile keyword guarantee?",
    "options": [
      "Atomic increments",
      "Visibility of a variable's changes across threads",
      "Thread ownership of the variable",
      "Immutability"
    ],
    "correctAnswer": 1,
    "explanation": "volatile ensures reads/writes bypass CPU caches so all threads see the latest value."
  },
  {
    "question": "What is a deadlock?",
    "options": [
      "A thread paused by the scheduler",
      "Two or more threads blocked forever waiting on each other's locks",
      "An exception thrown by the JVM",
      "A terminated daemon thread"
    ],
    "correctAnswer": 1,
    "explanation": "Deadlock arises from circular lock dependencies between threads."
  },
  {
    "question": "What does the synchronized keyword enforce?",
    "options": [
      "Parallel execution",
      "That only one thread enters a method or block at a time",
      "Immediate flushing of output",
      "Lazy class loading"
    ],
    "correctAnswer": 1,
    "explanation": "synchronized acquires a monitor lock providing mutual exclusion."
  },
  {
    "question": "What is ExecutorService?",
    "options": [
      "A servlet container",
      "A higher-level API for managing thread pools and async tasks",
      "A garbage collector mode",
      "A bytecode verifier"
    ],
    "correctAnswer": 1,
    "explanation": "ExecutorService abstracts thread creation and lifecycle behind pooled workers."
  },
  {
    "question": "What is CompletableFuture used for?",
    "options": [
      "Synchronous blocking I/O",
      "Building composable asynchronous computations with callbacks",
      "Defining final constants",
      "Serializing objects"
    ],
    "correctAnswer": 1,
    "explanation": "CompletableFuture chains thenApply/thenCompose style async pipelines."
  },
  {
    "question": "What does a fail-fast iterator do when its collection is structurally modified mid-iteration?",
    "options": [
      "Skips the new elements",
      "Throws ConcurrentModificationException",
      "Restarts iteration silently",
      "Blocks until modification completes"
    ],
    "correctAnswer": 1,
    "explanation": "Fail-fast iterators detect unexpected modifications and throw ConcurrentModificationException."
  },
  {
    "question": "Which is a benefit of String immutability in the JVM?",
    "options": [
      "Strings can store primitives",
      "Safe sharing across threads and cached hash codes",
      "Faster disk writes",
      "Automatic encryption"
    ],
    "correctAnswer": 1,
    "explanation": "Immutable Strings are thread-safe and their hash codes can be cached for hashing collections."
  },
  {
    "question": "What is string interning?",
    "options": [
      "Encrypting string literals",
      "Storing one shared copy of each distinct string in the pool",
      "Converting strings to byte arrays",
      "Inlining string methods"
    ],
    "correctAnswer": 1,
    "explanation": "Interning deduplicates identical string contents in the JVM string pool."
  },
  {
    "question": "How can a memory leak occur in Java despite garbage collection?",
    "options": [
      "GC never frees objects",
      "Objects remain reachable through forgotten static collections or listeners",
      "Primitives accumulate on the stack",
      "Bytecode grows over time"
    ],
    "correctAnswer": 1,
    "explanation": "Unintended live references prevent GC from reclaiming unused objects."
  },
  {
    "question": "What does the JVM Metaspace store?",
    "options": [
      "Heap objects",
      "Class metadata (replacing the older PermGen)",
      "Thread stacks",
      "String literals only"
    ],
    "correctAnswer": 1,
    "explanation": "Metaspace holds loaded-class metadata and grows outside the Java heap."
  },
  {
    "question": "What is double-checked locking used for?",
    "options": [
      "Verifying bytecode twice",
      "Reducing synchronization cost in lazy singleton initialization",
      "Locking files on disk",
      "Detecting deadlocks"
    ],
    "correctAnswer": 1,
    "explanation": "DCL checks the instance condition before and inside a synchronized block for lazy creation."
  },
  {
    "question": "What does the transient modifier mean for serialization?",
    "options": [
      "The field is encrypted",
      "The field is excluded from serialization",
      "The field becomes volatile",
      "The field must be static"
    ],
    "correctAnswer": 1,
    "explanation": "transient fields are skipped when the object is serialized."
  },
  {
    "question": "Which of these is a marker interface?",
    "options": [
      "Comparable",
      "Runnable",
      "Serializable",
      "AutoCloseable"
    ],
    "correctAnswer": 2,
    "explanation": "Serializable declares no methods; it only tags serializable types."
  },
  {
    "question": "What is the Java Reflection API used for?",
    "options": [
      "Rendering graphics",
      "Inspecting and invoking classes, methods, and fields at runtime",
      "Compiling annotations",
      "Pooling threads"
    ],
    "correctAnswer": 1,
    "explanation": "Reflection enables runtime introspection and dynamic invocation of members."
  },
  {
    "question": "What is the role of a ClassLoader?",
    "options": [
      "Compiling source files",
      "Loading class bytecode into the JVM at runtime",
      "Cleaning the heap",
      "Scheduling threads"
    ],
    "correctAnswer": 1,
    "explanation": "ClassLoaders locate and load .class bytes, linking them into running JVM state."
  },
  {
    "question": "What does <T extends Number> specify?",
    "options": [
      "T must be a superclass of Number",
      "T is restricted to Number or its subtypes",
      "T defaults to Integer",
      "T cannot be used with primitives"
    ],
    "correctAnswer": 1,
    "explanation": "Bounded type parameters constrain allowed generic arguments."
  },
  {
    "question": "What problem does Optional address?",
    "options": [
      "Slow I/O",
      "Explicit handling of possibly-absent values to avoid NullPointerException",
      "Memory leaks in collections",
      "Thread starvation"
    ],
    "correctAnswer": 1,
    "explanation": "Optional forces callers to acknowledge absence instead of dereferencing null."
  },
  {
    "question": "What is a method reference like String::isEmpty?",
    "options": [
      "A lambda shorthand referring to an existing method",
      "A deprecated syntax",
      "A way to override methods",
      "A serialization format"
    ],
    "correctAnswer": 0,
    "explanation": ":: creates function objects pointing at existing methods or constructors."
  },
  {
    "question": "How does Callable differ from Runnable?",
    "options": [
      "Callable is faster",
      "Callable can return a result and throw checked exceptions",
      "Callable cannot be submitted to executors",
      "Runnable accepts generics"
    ],
    "correctAnswer": 1,
    "explanation": "Callable<V>.call() yields a Future result and may throw checked exceptions."
  },
  {
    "question": "What does CountDownLatch allow?",
    "options": [
      "Counting method invocations",
      "Threads waiting until a count reaches zero before proceeding",
      "Measuring elapsed time",
      "Limiting file handles"
    ],
    "correctAnswer": 1,
    "explanation": "await() blocks until latch.countDown() drives the counter to zero."
  },
  {
    "question": "What does try-with-resources guarantee?",
    "options": [
      "Resources implementing AutoCloseable are closed automatically",
      "Exceptions are suppressed permanently",
      "Faster file reads",
      "Transactional rollback"
    ],
    "correctAnswer": 0,
    "explanation": "Resources declared in try(...) are closed in reverse order, even on failure."
  },
  {
    "question": "What is a WeakReference?",
    "options": [
      "A reference that prevents garbage collection",
      "A reference that does not stop its referent from being collected",
      "A synchronized pointer",
      "A compressed object header"
    ],
    "correctAnswer": 1,
    "explanation": "Weakly reachable objects may be collected once no strong references exist."
  },
  {
    "question": "What is the happens-before relationship in the Java Memory Model?",
    "options": [
      "A scheduling priority rule",
      "A visibility guarantee that one operation's effects are visible to another",
      "Class loading order",
      "Compilation sequence"
    ],
    "correctAnswer": 1,
    "explanation": "happens-before defines when writes in one thread are visible to reads in another."
  },
  {
    "question": "What is the difference between Comparable and Comparator?",
    "options": [
      "Comparable is external sorting logic; Comparator is built-in",
      "Comparable defines natural ordering inside the class; Comparator supplies external ordering",
      "Comparator only works with primitives",
      "They are interchangeable aliases"
    ],
    "correctAnswer": 1,
    "explanation": "Use Comparable for default order and Comparator for alternative/custom orders."
  },
  {
    "question": "What is a record (Java 16+)?",
    "options": [
      "A logging utility",
      "An immutable data carrier class with auto-generated accessors and equality",
      "A database driver",
      "A deprecated enum variant"
    ],
    "correctAnswer": 1,
    "explanation": "records concisely model immutable data with built-in constructor/accessors/equals."
  },
  {
    "question": "What is ThreadLocal used for?",
    "options": [
      "Sharing state between threads",
      "Giving each thread its own isolated copy of a variable",
      "Prioritizing threads",
      "Joining thread executions"
    ],
    "correctAnswer": 1,
    "explanation": "ThreadLocal variables hold independent values per thread."
  }
]
