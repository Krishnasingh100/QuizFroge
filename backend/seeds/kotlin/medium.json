[
  {
    "question": "What is the output?\n\nval words = listOf(\"hello\", \"world\")\nval result = words.filter { it.length > 4 }.map { it.uppercase() }\nprintln(result)",
    "options": ["[HELLO, WORLD]", "[hello, world]", "[HELLO]", "Compilation error"],
    "correctAnswer": 0,
    "explanation": "Both words have length > 4. map converts to uppercase: [HELLO, WORLD]."
  },
  {
    "question": "What is the output?\n\nval x = 5\nval y = x.let { it * 2 }\nPrintln(y)",
    "options": ["5", "10", "2.5", "Compilation error"],
    "correctAnswer": 1,
    "explanation": "let applies the lambda to x: 5 * 2 = 10."
  },
  {
    "question": "What is the output?\n\ndata class Person(val name: String, val age: Int)\nval p1 = Person(\"Alice\", 30)\nval p2 = Person(\"Alice\", 30)\nprintln(p1 == p2)",
    "options": ["true", "false", "Compilation error", "Runtime error"],
    "correctAnswer": 0,
    "explanation": "data class generates equals() based on properties, so equal objects return true."
  },
  {
    "question": "What is the output?\n\nval numbers = listOf(1, 2, 3, 4, 5)\nval sum = numbers.reduce { acc, i -> acc + i }\nprintln(sum)",
    "options": ["15", "10", "5", "Compilation error"],
    "correctAnswer": 0,
    "explanation": "reduce combines elements: ((1+2)+3)+4)+5 = 15."
  },
  {
    "question": "What is the output?\n\nsealed class Result {\n    class Success(val data: String) : Result()\n    class Error(val msg: String) : Result()\n}\nval r = Result.Success(\"ok\")\nwhen (r) {\n    is Result.Success -> println(r.data)\n    is Result.Error -> println(r.msg)\n}",
    "options": ["ok", "Error", "Compilation error", "Runtime error"],
    "correctAnswer": 0,
    "explanation": "r is Success, so the first branch executes, printing \"ok\"."
  },
  {
    "question": "What is the output?\n\nfun greet(name: String = \"World\") {\n    println(\"Hello, $name!\")\n}\ngreet()\ngreet(\"Kotlin\")",
    "options": ["Hello, World! Hello, Kotlin!", "Hello, Kotlin!", "Compilation error", "Runtime error"],
    "correctAnswer": 0,
    "explanation": "First call uses default parameter \"World\", second uses \"Kotlin\"."
  },
  {
    "question": "What is the output?\n\nval strings = listOf(\"a\", \"bb\", \"ccc\", \"dddd\")\nval grouped = strings.groupBy { it.length }\nprintln(grouped.keys)",
    "options": ["[1, 2, 3, 4]", "[a, bb, ccc, dddd]", "Compilation error", "Runtime error"],
    "correctAnswer": 0,
    "explanation": "groupBy groups by string length. keys are {1, 2, 3, 4}."
  },
  {
    "question": "What is the output?\n\nval x: Any = 42\nwhen (x) {\n    is Int -> println(\"Int: $x\")\n    is String -> println(\"String: $x\")\n    else -> println(\"Other\")\n}",
    "options": ["Int: 42", "String: 42", "Other", "Compilation error"],
    "correctAnswer": 0,
    "explanation": "x is an Int, so the first branch executes. Smart cast allows using x as Int."
  },
  {
    "question": "What is the output?\n\nval map = mapOf(\"a\" to 1, \"b\" to 2)\nprintln(map.getOrDefault(\"c\", 0))",
    "options": ["null", "0", "c", "Compilation error"],
    "correctAnswer": 1,
    "explanation": "Key \"c\" doesn't exist, so getOrDefault returns the default value 0."
  },
  {
    "question": "What is the output?\n\nfun factorial(n: Int): Int {\n    return if (n <= 1) 1 else n * factorial(n - 1)\n}\nprintln(factorial(5))",
    "options": ["120", "24", "60", "Compilation error"],
    "correctAnswer": 0,
    "explanation": "5! = 5 * 4 * 3 * 2 * 1 = 120."
  }
]